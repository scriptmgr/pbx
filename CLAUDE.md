# CLAUDE.md — pbx

This is a loader, not a spec. Read `AI.md` in full before doing any work
on this project — it carries both the product intent (WHAT) and the
implementation conventions (HOW) in one file; this project predates the
AI.md/IDEA.md split and has no separate `IDEA.md`.

`AI.md` supersedes the global `~/.claude/CLAUDE.md` defaults wherever
the two conflict.

See `TODO.AI.md` for the current outstanding work.
